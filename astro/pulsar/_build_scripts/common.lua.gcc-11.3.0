local sys_arch = os.getenv("SYS_ARCH") or ""
local root = pathJoin("/apps/users/pulsar/", sys_arch)
local version = myModuleVersion()
local pkgName = myModuleName()
local pkg = pathJoin(root,"gcc-11.3.0/software",pkgName,version)
append_path("PACKAGES", pkg, " ")
append_path("PATH", pathJoin(pkg, "bin"))
append_path("LD_LIBRARY_PATH", pathJoin(pkg, "lib"))
