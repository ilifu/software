Bootstrap: docker
From: ghcr.io/pangenome/pggb:{{ tag }}

%labels
  Version {{ tag }}
  pggb

%help
  The PanGenome Graph Builder (PGGB) is a tool for building a pangenome variation graphs from a set of input sequences.