---
# tasks file for containers
- name: Create Ubuntu Base Containers
  vars:
    - ubuntu_versions:
        - {
            version_name: bionic,
            version_number: 18.04,
        }
  include_tasks:
    file: ubuntu.yaml
  loop: "{{ ubuntu_versions }}"
  tags: always
