version = 1
revision = 2
requires-python = ">=3.11"

[[package]]
name = "pymlst"
version = "2.2"
source = { virtual = "." }

[package.metadata]
requires-dist = [{ name = "pymlst", specifier = ">=2.2" }]
